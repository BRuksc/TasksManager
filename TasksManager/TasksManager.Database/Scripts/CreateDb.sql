USE master
GO

BEGIN TRANSACTION

BEGIN TRY

	IF EXISTS (SELECT 1 FROM sys.databases WHERE name = 'TaskManager')
		DROP DATABASE TaskManager;

	CREATE DATABASE TaskManager

	COMMIT TRANSACTION;
END TRY

BEGIN CATCH
	ROLLBACK TRANSACTION;
	THROW;
END CATCH